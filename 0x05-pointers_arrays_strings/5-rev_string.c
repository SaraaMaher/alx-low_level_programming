#include <stdio.h>
/**
 * rev_string - reverses a string
 * @s: pointer
 * Return: void
 */
void rev_string(char *s)
{
int l, i;
char *v;
for (l = 0 ; s[l] != '\0' ; l++)
{
}
l--;
for (i = 0 ; i < l ; i++)
{
v[i] = s[l];
l--;
}
s = v;
}
