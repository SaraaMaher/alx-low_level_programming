#include <stdio.h>
/**
 * main - rints all arguments it receives
 * @argc: size
 * @argv: array
 * Return: always 0
 */
int main(int argc, char *argv[])
{
int i;
for (i = 0 ; i < argc ; i++)
printf("%s\n", arg[i]);
return (0);
}
